- name: Clone repository
  shell: git clone "https://github.com/loan-petit/portfolio.git"

- name: Start project
  command: docker-compose up -d --build --force-recreate
  args:
    chdir: ./portfolio
